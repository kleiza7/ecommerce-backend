version: "3.9"

services:
  postgres:
    image: postgres:16
    container_name: shopland-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: shopland_db
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
